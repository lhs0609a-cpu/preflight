# 없으면 인메모리로 뜬다. 서버리스에서는 요청마다 인스턴스가 달라
# 세션이 사라지므로, 배포에는 반드시 설정한다.
DATABASE_URL=postgres://user:pass@host:5432/preflight

# 링크에 들어갈 절대 주소. Vercel 에서는 비워두면 배포 도메인을 쓴다.
PF_BASE_URL=

# 개발용 데모 링크(/api/demo)를 운영에서도 열지 여부. 인증 없이 세션을
# 만드는 경로라 기본은 닫혀 있다. 07 §5.3 검증 때만 1 로 켠다.
PF_ALLOW_DEMO=
